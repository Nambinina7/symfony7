App\Entity\About:
  operations:
    get_about:
      class: ApiPlatform\Metadata\GetCollection
      method: GET
      security: 'is_granted("ROLE_EMPLOYER")'
      securityMessage: "Only employer roles can access the abouts data."

  normalizationContext:
    groups: [ 'about:read' ]